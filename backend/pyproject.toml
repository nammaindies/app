[project]
name = "indiedex-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "uuid-utils>=0.17.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
